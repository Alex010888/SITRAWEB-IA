# URL base de la API (sin /api al final; el cliente añade /api en cada petición)
VITE_API_BASE_URL=http://localhost/sitra_web/backend/public

# URL del sitio público (landing). Si está definida, se muestra "Ver sitio público" en el panel.
# VITE_LANDING_URL=http://localhost/sitra_web/public
